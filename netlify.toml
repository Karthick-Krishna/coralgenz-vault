# Netlify configuration for SecureVault
# Automatically configures build commands, publish directories, and routing redirects.

[build]
  command = "npm run build"
  publish = "dist"

# Redirects to support Single Page Application (SPA) routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
